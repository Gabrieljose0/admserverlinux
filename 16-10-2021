sudo su
senha
apt update &&
apt upgrade -y &&
apt install apache2 -y &&
sudo apt install openssh-client -y &&
sudo apt install openssh-server -y 


https://github.com/huntercodecamp

sudo nano -w / etc / ssh / ssh_config
sudo nano -w / etc / ssh / sshd_config
serviço apache2 start && 
serviço chamado start 


https://ninite.com/
massa de vidraceiro
l1professor @ l1professor
